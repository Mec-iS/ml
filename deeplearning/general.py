"""
Basic principles:
* weights and biasis in linear model: wX + b
    Find good initialization values
 * Well-conditioned: try each variable to have zero mean and equal variance:
    Xi = 0
    sigma(Xi) == sigma(Xj)
"""